import '../styles/Home.css'


const Home = () => {
    return (
        <div className="hero-section">
            <h1>Home Page(substitute for landing page)</h1>

        </div>

    );
};

export default Home;